# Optional: Tavily API key for richer search context during discovery
# Get from https://app.tavily.com (free tier: 1,000 searches/month)
# If not set, agents use Claude's built-in WebSearch tool instead
TAVILY_API_KEY=
