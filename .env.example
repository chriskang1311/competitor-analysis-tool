# Anthropic API key — get from https://console.anthropic.com/keys
ANTHROPIC_API_KEY=

# Tavily API key — get from https://app.tavily.com (free tier: 1,000 searches/month)
# Optional: if not set, discovery falls back to a single Claude WebSearch agent
TAVILY_API_KEY=
